# CMS 历史记录功能说明文档

## 一、概述

CMS 系统提供完整的历史记录功能，用于追踪所有内容修改和操作历史。历史记录功能通过 `UnifiedHistoryModal` 组件实现，支持三种不同的记录模式。

---

## 二、历史记录类型

### 2.1 配置页面模式 (mode="editor")

**适用场景**：单一配置页面，内容始终存在，通过"保存配置"和"发布更新"进行版本管理。

**记录类型**：
- **保存配置** (`draft`) - 用户点击"保存配置"按钮时记录
- **发布上线** (`published`) - 用户点击"发布更新"按钮时记录

**数据结构**：
```javascript
{
  id: string,              // 记录唯一ID
  time: string,            // 操作时间，格式：'YYYY-MM-DD HH:mm'
  description: string,     // 修改描述，如："更新了主标题和Banner图片"
  operator: string,        // 操作人，如："admin"
  status: 'draft' | 'published'  // 状态：'draft'=保存配置，'published'=发布上线
}
```

**功能特性**：
- ✅ 支持恢复历史版本（非最新版本可恢复）
- ✅ 按月份分组显示
- ✅ 支持搜索、时间筛选、状态筛选
- ✅ 显示操作人和操作时间

**应用页面**：

#### 2.1.1 全局资源库
- ✅ **公司档案信息** (`CompanyInfoEditor`)
  - 页面标识：`company_info`
  - 记录内容：公司档案数据的保存和发布记录

#### 2.1.2 首页管理
- ✅ **Banner 设置** (`HomeBannerEditor`)
  - 页面标识：`home_banner`
  - 记录内容：首页Banner配置的保存和发布记录
  
- ✅ **最新动态** (`HomeNewsEditor`)
  - 页面标识：`home_news`
  - 记录内容：首页最新动态配置的保存和发布记录
  
- ✅ **关于我们（首页）** (`HomeAboutEditor`)
  - 页面标识：`home_about`
  - 记录内容：首页关于我们模块配置的保存和发布记录
  
- ✅ **业务板块** (`HomeBusinessEditor`)
  - 页面标识：`home_business`
  - 记录内容：首页业务板块配置的保存和发布记录
  
- ✅ **社会责任与文化** (`HomeCultureEditor`)
  - 页面标识：`home_culture`
  - 记录内容：首页社会责任与文化轮播配置的保存和发布记录
  
- ✅ **员工风采** (`HomeStaffEditor`)
  - 页面标识：`home_staff`
  - 记录内容：首页员工风采轮播配置的保存和发布记录
  
- ✅ **合作伙伴** (`HomePartnerEditor`)
  - 页面标识：`home_partner`
  - 记录内容：首页合作伙伴Logo配置的保存和发布记录

#### 2.1.3 关于我们
- ✅ **Banner 设置** (`AboutBannerEditor`)
  - 页面标识：`about_banner`
  - 记录内容：关于我们频道Banner配置的保存和发布记录
  
- ✅ **公司简介** (`IntroEditor`)
  - 页面标识：`about_intro`
  - 记录内容：公司简介页面配置的保存和发布记录
  
- ✅ **发展历程** (`HistoryEditor`)
  - 页面标识：`about_history`
  - 记录内容：发展历程时间线配置的保存和发布记录
  
- ✅ **董事长致辞** (`SpeechEditor`)
  - 页面标识：`about_speech`
  - 记录内容：董事长致辞页面配置的保存和发布记录
  
- ✅ **公司领导** (`LeadersEditor`)
  - 页面标识：`about_leaders`
  - 记录内容：公司领导展示配置的保存和发布记录
  
- ✅ **荣誉资质** (`HonorsEditor`)
  - 页面标识：`about_honors`
  - 记录内容：荣誉资质页面配置的保存和发布记录

#### 2.1.4 新闻中心
- ✅ **Banner 设置** (`NewsBannerEditor`)
  - 页面标识：`news_banner`
  - 记录内容：新闻中心频道Banner配置的保存和发布记录
  - 说明：使用 `PageBanner` 组件，手动集成历史记录功能

#### 2.1.5 业务领域
- ✅ **Banner 设置** (`BusinessBannerEditor`)
  - 页面标识：`business_banner`
  - 记录内容：业务领域频道Banner配置的保存和发布记录
  
- ✅ **实业投资** (`BusinessIndustryEditor`)
  - 页面标识：`business_industry`
  - 记录内容：实业投资业务模块配置的保存和发布记录
  
- ✅ **国际贸易** (`BusinessTradeEditor`)
  - 页面标识：`business_trade`
  - 记录内容：国际贸易业务模块配置的保存和发布记录

#### 2.1.6 品牌文化
- ✅ **Banner 设置** (`ModuleBannerEditor`)
  - 页面标识：`culture_banner`
  - 记录内容：品牌文化频道Banner配置的保存和发布记录
  
- ✅ **企业文化** (`CultureCompanyEditor`)
  - 页面标识：`culture_company`
  - 记录内容：企业文化页面配置的保存和发布记录
  - **页面整体历史记录**：
    - 历史记录是**3个Tab整体**的，包括：
      - **核心价值观** Tab（企业理念、价值理念卡片）
      - **精彩文化活动** Tab（文化活动列表）
      - **员工风采** Tab（员工风采展示）
    - 保存和发布时，会将三个Tab的所有数据（`values`, `activities`, `staff`）一起保存
    - 历史记录记录的是整个页面的版本，而不是单个Tab的版本
    - 入口位置：页面顶部右侧（`PageBanner` 右侧"历史记录"按钮）
  - **单个活动历史记录**：
    - 每个活动都有独立的版本历史记录
    - 入口位置：
      1. 活动卡片右上角时钟图标按钮（悬停显示）
      2. 活动编辑弹窗底部右侧"历史记录"按钮（仅编辑已有活动时显示）
    - 模式：`mode="editor"`，`useArticleVersionLabels={true}`
    - 页面标识：`culture_activity_{activityId}`
  
- ✅ **品牌形象** (`BrandImageEditor`)
  - 页面标识：`culture_brand`
  - 记录内容：品牌形象页面配置的保存和发布记录

#### 2.1.7 党建领航
- ✅ **Banner 设置** (`ModuleBannerEditor`)
  - 页面标识：`party_banner`
  - 记录内容：党建领航频道Banner配置的保存和发布记录
  
- ✅ **党的建设** (`PartyBuildingEditor`)
  - 页面标识：`party_building`
  - 记录内容：党的建设文章管理配置的保存和发布记录
  - 说明：文章类页面，可能需要使用文章页面级操作模式
  
- ✅ **廉洁从业** (`PartyIntegrityEditor`)
  - 页面标识：`party_integrity`
  - 记录内容：廉洁从业页面配置的保存和发布记录
  - 说明：过渡页面，可能暂未实现历史记录功能
  
- ✅ **青年之友** (`PartyYouthEditor`)
  - 页面标识：`party_youth`
  - 记录内容：青年之友文章管理配置的保存和发布记录
  - 说明：文章类页面，可能需要使用文章页面级操作模式
  
- ✅ **职工之家** (`PartyWorkersEditor`)
  - 页面标识：`party_workers`
  - 记录内容：职工之家页面配置的保存和发布记录
  - 说明：过渡页面，可能暂未实现历史记录功能

#### 2.1.8 人力资源
- ✅ **Banner 设置** (`ModuleBannerEditor`)
  - 页面标识：`hr_banner`
  - 记录内容：人力资源频道Banner配置的保存和发布记录
  
- ✅ **人才战略** (`HREditor`, type='strategy')
  - 页面标识：`hr_strategy`
  - 记录内容：人才战略页面配置的保存和发布记录
  
- ✅ **人才队伍** (`HREditor`, type='team')
  - 页面标识：`hr_team`
  - 记录内容：人才队伍页面配置的保存和发布记录
  
- ✅ **招聘信息** (`HRRecruitEditor`)
  - 页面标识：`hr_recruit`
  - 记录内容：招聘信息管理配置的保存和发布记录
  - 说明：列表类页面，可能需要使用资源库模式或文章页面级操作模式

#### 2.1.9 企业公开
- ✅ **Banner 设置** (`ModuleBannerEditor`)
  - 页面标识：`public_banner`
  - 记录内容：企业公开频道Banner配置的保存和发布记录
  
#### 2.1.10 企业公开（续）
- ✅ **基本信息** (`PublicBasicEditor`)
  - 页面标识：`public_basic`
  - 记录内容：企业基本信息各Tab（工商注册、企业资质、发展战略、业务分布、工程业绩）的保存和发布记录
  - 特殊说明：每个Tab独立记录历史
  
- ✅ **联系方式** (`PublicContactEditor`)
  - 页面标识：`public_contact`
  - 记录内容：联系方式配置的保存和发布记录

#### 2.1.11 可持续发展
- ✅ **Banner 设置** (`ModuleBannerEditor`)
  - 页面标识：`sustain_banner`
  - 记录内容：可持续发展频道Banner配置的保存和发布记录
  
#### 2.1.12 可持续发展（续）
- ✅ **科技创新** (`SustainTechEditor`)
  - 页面标识：`sustain_tech`
  - 记录内容：科技创新页面配置的保存和发布记录
  - 特殊说明：包含"科技简介"、"荣誉资质展示"、"科技创新成果"三个Tab的历史记录
  
- ✅ **安环行动** (`SustainSafetyEditor`)
  - 页面标识：`sustain_safety`
  - 记录内容：安环行动页面配置的保存和发布记录
  
- ✅ **全球发展** (`SustainGlobalEditor`)
  - 页面标识：`sustain_global`
  - 记录内容：全球发展页面配置的保存和发布记录
  
- ✅ **社会责任** (`SustainResponsibilityEditor`)
  - 页面标识：`sustain_responsibility`
  - 记录内容：社会责任页面配置的保存和发布记录
  
- ✅ **ESG报告** (`SustainESGEditor`)
  - 页面标识：`sustain_esg`
  - 记录内容：ESG报告文件上传和配置的保存和发布记录

#### 2.1.13 其他
- ✅ **页脚内容** (`FooterContentEditor`)
  - 页面标识：`footer_content`
  - 记录内容：页脚内容配置的保存和发布记录

---

### 2.2 资源库页面级操作日志 (mode="library")

**适用场景**：资源库管理页面，记录对资源项的新增、编辑、删除操作。

**记录类型**：
- **新增** (`add`) - 新增资源项时记录
- **编辑** (`edit`) - 编辑资源项时记录
- **删除** (`delete`) - 删除资源项时记录

**数据结构**：
```javascript
{
  id: string,              // 记录唯一ID
  time: string,            // 操作时间，格式：'YYYY-MM-DD HH:mm'
  description: string,     // 操作描述，如："新增了 [图片名称]"
  itemTitle: string,       // 资源项名称，如："公司Logo.png"
  operator: string,        // 操作人，如："admin"
  action: 'add' | 'edit' | 'delete'  // 操作类型
}
```

**功能特性**：
- ❌ 不支持恢复历史版本（资源库操作不可逆）
- ✅ 按月份分组显示
- ✅ 支持搜索、时间筛选、操作类型筛选
- ✅ 显示操作人和操作时间

**应用页面**：

#### 2.2.1 全局资源库
- ✅ **图片媒体库** (`ImageLibraryEditor`)
  - 页面标识：`image_library`
  - 记录内容：图片的新增、编辑、删除操作
  - 示例记录：
    - `{ action: 'add', description: '新增了 [公司Logo.png]', itemTitle: '公司Logo.png' }`
    - `{ action: 'edit', description: '编辑了 [Banner背景图.jpg]', itemTitle: 'Banner背景图.jpg' }`
    - `{ action: 'delete', description: '删除了 [旧版Logo.png]', itemTitle: '旧版Logo.png' }`

- ✅ **资质荣誉库** (`CertLibraryEditor`)
  - 页面标识：`cert_library`
  - 记录内容：资质证书和荣誉奖项的新增、编辑、删除操作
  - 示例记录：
    - `{ action: 'add', description: '新增了 [工程设计综合甲级资质]', itemTitle: '工程设计综合甲级资质' }`
    - `{ action: 'edit', description: '编辑了 [2023年度优秀企业奖]', itemTitle: '2023年度优秀企业奖' }`
    - `{ action: 'delete', description: '删除了 [过期资质证书]', itemTitle: '过期资质证书' }`

- ✅ **人员专家库** (`PeopleLibraryEditor`)
  - 页面标识：`people_library`
  - 记录内容：人员信息的新增、编辑、删除操作
  - 示例记录：
    - `{ action: 'add', description: '新增了 [张三 - 董事长]', itemTitle: '张三 - 董事长' }`
    - `{ action: 'edit', description: '编辑了 [李四 - 总经理]', itemTitle: '李四 - 总经理' }`
    - `{ action: 'delete', description: '删除了 [王五 - 副总经理]', itemTitle: '王五 - 副总经理' }`

- ✅ **公司档案信息** (`CompanyInfoEditor`)
  - 页面标识：`company_info`
  - 记录内容：公司档案数据的新增、编辑、删除操作
  - 示例记录：
    - `{ action: 'add', description: '新增了 [业务覆盖国家/地区]', itemTitle: '业务覆盖国家/地区' }`
    - `{ action: 'edit', description: '编辑了 [全球员工]', itemTitle: '全球员工' }`
    - `{ action: 'delete', description: '删除了 [占地面积]', itemTitle: '占地面积' }`

#### 2.2.2 业务领域
- ⚠️ **注意**：工程承包和项目总览的页面级操作已迁移到 `mode="article"` 模式，详见 2.3.2 和 2.3.3

---

### 2.2.3 资源库单个资源版本历史

**适用场景**：资源库中单个资源项的版本历史记录，用于追踪该资源项的修改历史。

**组件**：`VersionHistoryModal`

**数据结构**：
```javascript
{
  version: string,         // 版本号，如："V1", "V2", "V3"
  time: string,            // 操作时间，格式：'YYYY-MM-DD HH:mm'
  operator: string,        // 操作人，如："admin"
  changes: string          // 修改内容描述，如："更新了数值为2200"、"初始创建"
}
```

**功能特性**：
- ✅ 支持恢复到历史版本（非最新版本可恢复）
- ✅ 时间轴样式展示，当前版本用蓝色圆点标记
- ✅ 显示"当前"标签标记最新版本
- ✅ 显示版本号、修改内容、操作人、时间
- ✅ 历史版本显示"恢复"按钮

**应用页面**：

#### 2.2.3.1 全局资源库
- ✅ **公司档案信息** (`CompanyInfoEditor`)
  - 入口位置：数据卡片右上角操作按钮中的"历史记录"图标（`ResourceActions` 组件）
  - 显示内容：单个数据项的版本历史（如：成立时间、全球员工、建成项目等）
  - 资源类型：数据展示项（`isStat: true`）
  - 示例版本历史：
    ```javascript
    [
      { version: 'V2', time: '2025-12-10 14:30', operator: 'admin', changes: '更新了成立时间为1964-10-01' },
      { version: 'V1', time: '2025-10-01 09:00', operator: 'admin', changes: '初始创建' }
    ]
    ```

- ✅ **资质荣誉库** (`CertLibraryEditor`)
  - 入口位置：数据展示卡片右上角操作按钮中的"历史记录"图标（`ResourceActions` 组件）
  - 显示内容：单个资质数据的版本历史
  - 资源类型：数据展示项（`isStat: true`）
  - 示例版本历史：
    ```javascript
    [
      { version: 'V2', time: '2025-12-06 10:00', operator: 'admin', changes: '更新了数值为9' },
      { version: 'V1', time: '2025-10-01 09:00', operator: 'admin', changes: '初始创建' }
    ]
    ```

**版本历史弹窗规范**：
- **标题**：显示"版本历史"
- **资源信息**：显示资源项名称（如："工程勘察综合甲级资质"）
- **资源类型标签**：数据展示项显示"数据展示项"标签（灰色背景）
- **版本列表**：
  - 时间轴样式，左侧垂直连接线
  - 当前版本：蓝色圆点（`bg-brand`），版本号蓝色文字，显示"当前"标签
  - 历史版本：灰色圆点（`bg-gray-4`），版本号黑色文字
  - 每个版本显示：版本号、修改内容、操作人和时间
  - 历史版本右侧显示"恢复"按钮（时钟图标 + 文字）
- **底部按钮**：显示"关闭"按钮

**恢复功能**：
- 点击历史版本的"恢复"按钮，弹出确认对话框
- 确认后，从版本描述中提取数据并恢复到该版本
- 恢复后需要手动保存才能生效

---

### 2.3 文章页面级操作模式 (mode="article")

**适用场景**：文章管理页面，记录文章的发布、下架、更新等页面级操作。

**记录类型**：
- **发布** (`publish`) - 文章发布上线时记录
- **下架** (`unpublish`) - 文章下架时记录
- **更新** (`update`) - 文章更新时记录（已发布文章重新保存）

**数据结构**：
```javascript
{
  id: string,              // 记录唯一ID
  time: string,            // 操作时间，格式：'YYYY-MM-DD HH:mm'
  description: string,     // 操作描述，如："发布了《新闻标题》"、"下架了《新闻标题》"、"更新了《新闻标题》"
  operator: string,        // 操作人，如："admin"
  action: 'publish' | 'unpublish' | 'update'  // 操作类型
}
```

**功能特性**：
- ❌ **不支持恢复历史版本**（页面级操作日志，不可逆）
- ✅ 按月份分组显示
- ✅ 支持搜索、时间筛选、操作类型筛选
- ✅ 显示操作人和操作时间

**应用页面**：

#### 2.3.1 新闻中心
- ✅ **文章管理 - 页面级操作** (`NewsListEditor`)
  - 页面标识：`news_list_page`
  - 记录内容：新闻文章的发布、下架、更新操作
  - 入口位置：`PageBanner` 右侧"历史记录"按钮
  - 示例记录：
    - `{ action: 'publish', description: '发布了《公司荣获2024年度优秀企业奖》', time: '2024-03-20 14:30', operator: 'admin' }`
    - `{ action: 'unpublish', description: '下架了《2023年度工作总结》', time: '2024-03-18 10:00', operator: 'admin' }`
    - `{ action: 'update', description: '更新了《新产品发布会》', time: '2024-03-15 16:20', operator: 'admin' }`

#### 2.3.2 工程承包
- ✅ **工程承包 - 页面级操作** (`BusinessEngineeringEditor`)
  - 页面标识：`business_engineering_page`
  - 记录内容：工程承包项目的发布、下架、更新操作
  - 入口位置：`PageBanner` 右侧"历史记录"按钮
  - 示例记录：
    - `{ action: 'publish', description: '发布了《某大型化肥装置建设项目》', time: '2024-03-20 14:30', operator: 'admin' }`
    - `{ action: 'unpublish', description: '下架了《某炼化一体化项目》', time: '2024-03-18 10:00', operator: 'admin' }`
    - `{ action: 'update', description: '更新了《某乙烯装置EPC总承包项目》', time: '2024-03-15 16:20', operator: 'admin' }`

#### 2.3.3 项目总览
- ✅ **项目总览 - 页面级操作** (`BusinessProjectEditor`)
  - 页面标识：`business_project_page`
  - 记录内容：项目总览项目的发布、下架、更新操作
  - 入口位置：`PageBanner` 右侧"历史记录"按钮
  - 示例记录：
    - `{ action: 'publish', description: '发布了《双溪科技工业园配套基础设施项目》', time: '2024-03-20 14:30', operator: 'admin' }`
    - `{ action: 'unpublish', description: '下架了《龙泉驿区世纪广场城市停车场建设项目》', time: '2024-03-18 10:00', operator: 'admin' }`
    - `{ action: 'update', description: '更新了《中船（天津）船舶制造有限公司工程承揽合同》', time: '2024-03-15 16:20', operator: 'admin' }`

#### 2.3.4 单篇文章/项目版本历史
- ✅ **单篇文章版本历史** (`NewsListEditor` - 文章详情弹窗)
  - 页面标识：`news_article_{articleId}`
  - 记录内容：单篇文章的编辑和发布版本历史
  - 模式：`mode="editor"`（使用配置页面模式）
  - 入口位置：编辑弹窗底部右侧"历史记录"按钮（仅编辑已有文章时显示）
  - 特殊配置：`useArticleVersionLabels={true}`（使用文章版本历史标签：保存、发布、更新、下架）
  - 示例记录：
    - `{ status: 'published', description: '发布了文章', time: '2024-03-20 14:30', operator: 'admin' }`
    - `{ status: 'draft', description: '编辑了"标题"、"摘要"内容', time: '2024-03-20 14:25', operator: 'admin' }`
    - `{ status: 'updated', description: '更新了文章', time: '2024-03-19 10:00', operator: 'admin' }`
    - `{ status: 'unpublish', description: '', time: '2024-03-18 09:00', operator: 'admin' }`（下架记录无辅助文字）

- ✅ **单项目版本历史** (`BusinessEngineeringEditor` / `BusinessProjectEditor` - 项目详情弹窗)
  - 页面标识：`business_project_{projectId}`
  - 记录内容：单个项目的编辑和发布版本历史
  - 模式：`mode="editor"`（使用配置页面模式）
  - 入口位置：编辑弹窗底部右侧"历史记录"按钮（仅编辑已有项目时显示）
  - 特殊配置：`useArticleVersionLabels={true}`
  - 示例记录：同单篇文章版本历史格式

- ✅ **单活动版本历史** (`CultureCompanyEditor` - 活动详情弹窗)
  - 页面标识：`culture_activity_{activityId}`
  - 记录内容：单个活动的编辑和发布版本历史
  - 模式：`mode="editor"`（使用配置页面模式）
  - 入口位置：
    1. 活动卡片右上角时钟图标按钮（悬停显示）
    2. 活动编辑弹窗底部右侧"历史记录"按钮（仅编辑已有活动时显示）
  - 特殊配置：`useArticleVersionLabels={true}`
  - 示例记录：同单篇文章版本历史格式

---

## 三、特殊场景说明

### 3.1 工程承包项目版本历史

**页面**：`BusinessEngineeringEditor` - 单个项目编辑弹窗

**模式**：`mode="editor"`

**记录内容**：单个工程承包项目的编辑和发布版本历史

**数据结构**：同配置页面模式

**示例记录**：
```javascript
[
  { id: 'v1', time: '2024-03-20 14:30', description: '更新了《某大型化肥装置建设项目》的内容', operator: 'admin', status: 'published' },
  { id: 'v2', time: '2024-03-20 14:25', description: '编辑了《某大型化肥装置建设项目》', operator: 'admin', status: 'draft' },
  { id: 'v3', time: '2024-03-15 10:00', description: '首次发布《某大型化肥装置建设项目》', operator: 'admin', status: 'published' },
]
```

### 3.2 项目总览项目版本历史

**页面**：`BusinessProjectEditor` - 单个项目编辑弹窗

**模式**：`mode="editor"`

**记录内容**：单个项目总览项目的编辑和发布版本历史

**数据结构**：同配置页面模式

### 3.3 企业文化页面说明

**页面**：`CultureCompanyEditor`

**说明**：
- 企业文化页面包含3个Tab：核心价值观、精彩文化活动、员工风采
- **页面整体历史记录**：
  - 通过页面顶部右侧的"历史记录"按钮查看（`PageBanner` 右侧）
  - 记录整个页面的保存和发布历史，包括所有3个Tab的数据（`values`, `activities`, `staff`）
  - 模式：`mode="editor"`
- **单个活动历史记录**：
  - 每个活动都有独立的版本历史记录
  - 入口位置：
    1. 活动卡片右上角时钟图标按钮（悬停显示）
    2. 活动编辑弹窗底部右侧"历史记录"按钮（仅编辑已有活动时显示）
  - 模式：`mode="editor"`，`useArticleVersionLabels={true}`
  - 记录内容：该活动的编辑和发布版本历史（保存、发布、更新、下架）
- **员工风采**：目前没有单个员工的历史记录，只有页面整体的历史记录

### 3.4 科技创新成果模块历史记录

**页面**：`SustainTechEditor` - "科技创新成果"Tab

**模式**：`mode="library"`

**记录内容**：科技创新成果模块的新增、编辑、删除操作

**数据结构**：同资源库页面级操作日志

### 3.5 全球发展团队结构历史记录

**页面**：`SustainGlobalEditor` - "发展团队结构"模块

**模式**：`mode="library"`

**记录内容**：团队结构项的新增、编辑、删除操作

**数据结构**：同资源库页面级操作日志

### 3.6 资源库单个资源版本历史

**组件**：`VersionHistoryModal`

**适用场景**：资源库中单个资源项的版本历史记录

**功能特性**：
- ✅ 支持恢复到历史版本
- ✅ 时间轴样式展示
- ✅ 显示当前版本和历史版本
- ✅ 历史版本可恢复

**应用页面**：
- ✅ **公司档案信息** - 数据展示项的版本历史
- ✅ **资质荣誉库** - 资质数据的版本历史

**数据结构**：见 4.2.1 节

---

## 四、历史记录数据结构规范

### 4.1 配置页面模式记录结构

```typescript
interface EditorHistoryRecord {
  id: string;                    // 记录唯一ID，格式：'h{timestamp}_{index}'
  time: string;                   // 操作时间，ISO格式：'YYYY-MM-DD HH:mm'
  description: string;            // 修改描述，用户可自定义或系统自动生成
  operator: string;               // 操作人用户名
  status: 'draft' | 'published'; // 状态：'draft'=保存配置，'published'=发布上线
  data?: object;                 // 可选：该版本的数据快照（用于恢复）
}
```

### 4.2 资源库页面级操作日志记录结构

```typescript
interface LibraryHistoryRecord {
  id: string;                    // 记录唯一ID，格式：'lib_{timestamp}_{index}'
  time: string;                   // 操作时间，ISO格式：'YYYY-MM-DD HH:mm'
  description: string;            // 操作描述，系统自动生成
  itemTitle: string;              // 资源项名称
  itemId?: string;               // 可选：资源项ID
  operator: string;              // 操作人用户名
  action: 'add' | 'edit' | 'delete'; // 操作类型
  data?: object;                 // 可选：操作时的数据快照
}
```

### 4.2.1 单个资源版本历史记录结构

```typescript
interface ResourceVersionHistory {
  version: string;                // 版本号，格式：'V1', 'V2', 'V3'...
  time: string;                   // 操作时间，ISO格式：'YYYY-MM-DD HH:mm'
  operator: string;              // 操作人用户名
  changes: string;               // 修改内容描述，如："更新了数值为2200"、"初始创建"
  data?: object;                 // 可选：该版本的数据快照（用于恢复）
}
```

### 4.3 文章页面级操作记录结构

```typescript
interface ArticleHistoryRecord {
  id: string;                    // 记录唯一ID，格式：'art_{timestamp}_{index}'
  time: string;                   // 操作时间，ISO格式：'YYYY-MM-DD HH:mm'
  description: string;            // 操作描述，系统自动生成
  articleId?: number;            // 可选：文章ID
  articleTitle?: string;          // 可选：文章标题
  operator: string;             // 操作人用户名
  action: 'publish' | 'unpublish' | 'move'; // 操作类型
  fromPosition?: string;          // 可选：移动前的展示位置
  toPosition?: string;            // 可选：移动后的展示位置
  data?: object;                 // 可选：操作时的数据快照（用于恢复）
}
```

---

## 五、历史记录生成规则

### 5.1 配置页面模式

**保存配置时**：
- 自动生成 `draft` 状态记录
- 描述建议：系统自动检测修改的字段，生成描述，如："更新了主标题和Banner图片"
- 如果用户未修改描述，使用默认描述："保存了配置修改"

**发布更新时**：
- 自动生成 `published` 状态记录
- 描述建议：与对应的 `draft` 记录描述保持一致
- 如果存在未发布的 `draft` 记录，将 `draft` 记录标记为已发布

### 5.2 资源库页面级操作日志

**新增资源时**：
- 自动生成 `add` 操作记录
- 描述格式：`"新增了 [${itemTitle}]"`

**编辑资源时**：
- 自动生成 `edit` 操作记录
- 描述格式：`"编辑了 [${itemTitle}]"`
- 同时生成单个资源的版本历史记录（`VersionHistoryModal`）

**删除资源时**：
- 自动生成 `delete` 操作记录
- 描述格式：`"删除了 [${itemTitle}]"`

### 5.2.1 单个资源版本历史生成规则

**编辑资源时**：
- 自动生成版本历史记录
- 版本号：自动递增（V1, V2, V3...）
- 修改内容：系统自动生成描述，如："更新了数值为2200"、"更新了成立时间为1964-10-01"
- 初始创建：首次创建时记录为"初始创建"
- 操作人：记录当前操作人
- 时间：记录操作时间

### 5.3 文章页面级操作模式

**发布文章时**：
- 自动生成 `publish` 操作记录
- 描述格式：`"发布了《${articleTitle}》"`

**下架文章时**：
- 自动生成 `unpublish` 操作记录
- 描述格式：`"下架了《${articleTitle}》"`

**更新文章时**：
- 自动生成 `update` 操作记录
- 描述格式：`"更新了《${articleTitle}》"`
- 触发条件：已发布文章重新保存时

---

## 六、历史记录存储建议

### 6.1 本地存储（当前实现）

- 使用 `localStorage` 存储历史记录
- Key 格式：`cms_history_{pageKey}`
- 数据格式：JSON 字符串化的数组
- 限制：最多保存最近 100 条记录

### 6.2 后端存储（未来实现）

- 使用数据库存储历史记录
- 表结构建议：
  - `id` (主键)
  - `page_key` (页面标识)
  - `record_type` (记录类型：editor/library/article)
  - `time` (操作时间)
  - `description` (描述)
  - `operator` (操作人)
  - `status` / `action` (状态/操作类型)
  - `data_snapshot` (数据快照，JSON格式)
  - `created_at` (创建时间)

---

## 七、历史记录功能入口

历史记录按钮在系统中的位置和触发方式如下：

### 7.1 配置页面历史记录入口

**位置类型**：`PageBanner` 组件右侧

**适用页面**：
- ✅ **公司档案信息** (`CompanyInfoEditor`) - 页面顶部右侧
- ✅ **首页管理** - 所有子页面（Banner设置、最新动态、关于我们、业务板块、社会责任与文化、员工风采、合作伙伴）
- ✅ **关于我们** - 所有子页面（Banner设置、公司简介、发展历程、董事长致辞、公司领导、荣誉资质）
- ✅ **新闻中心 - Banner设置** (`NewsBannerEditor`) - 页面顶部右侧
- ✅ **业务领域** - Banner设置、实业投资、国际贸易
- ✅ **品牌文化** - Banner设置、品牌形象
- ✅ **党建领航** - Banner设置
- ✅ **人力资源** - Banner设置、人才战略、人才队伍
- ✅ **企业公开** - Banner设置、联系方式
- ✅ **可持续发展** - Banner设置、科技创新、安环行动、全球发展、社会责任、ESG报告
- ✅ **页脚内容** (`FooterContentEditor`) - 页面顶部右侧

**触发方式**：点击 `PageBanner` 右侧的"历史记录"按钮（时钟图标 + 文字）

**显示内容**：该页面的所有保存和发布记录（`mode="editor"`）

---

### 7.2 资源库页面级操作日志入口

**位置类型**：`PageBanner` 组件右侧

**适用页面**：
- ✅ **图片媒体库** (`ImageLibraryEditor`) - 页面顶部右侧
- ✅ **资质荣誉库** (`CertLibraryEditor`) - 页面顶部右侧
- ✅ **人员专家库** (`PeopleLibraryEditor`) - 页面顶部右侧
- ✅ **公司档案信息** (`CompanyInfoEditor`) - 页面顶部右侧

**触发方式**：点击 `PageBanner` 右侧的"历史记录"按钮

**显示内容**：该资源库的所有操作记录（新增、编辑、删除，`mode="library"`）

---

### 7.2.1 资源库单个资源版本历史入口

**位置类型1**：资源卡片右上角操作按钮（`ResourceActions` 组件）

**适用页面**：
- ✅ **公司档案信息** (`CompanyInfoEditor`) - 数据卡片右上角
- ✅ **资质荣誉库** (`CertLibraryEditor`) - 数据展示卡片右上角

**触发方式**：点击资源卡片右上角的"历史记录"图标按钮（悬停显示）

**显示内容**：该资源项的版本历史（`VersionHistoryModal` 组件）

**显示条件**：仅当资源项已存在时显示（已有版本历史）

---

### 7.3 文章管理页面历史记录入口

#### 7.3.1 页面级操作历史

**位置类型**：`PageBanner` 组件右侧

**适用页面**：
- ✅ **新闻中心 - 文章管理** (`NewsListEditor`) - 页面顶部右侧
- ✅ **工程承包** (`BusinessEngineeringEditor`) - 页面顶部右侧
- ✅ **项目总览** (`BusinessProjectEditor`) - 页面顶部右侧

**触发方式**：点击 `PageBanner` 右侧的"历史记录"按钮

**显示内容**：页面级操作记录（发布、下架、更新，`mode="article"`）

#### 7.3.2 单篇文章/项目版本历史

**位置类型1**：编辑弹窗底部右侧（`EditorStatusBar` 组件）

**适用页面**：
- ✅ **新闻中心 - 文章管理** (`NewsListEditor`) - 编辑文章弹窗底部
- ✅ **工程承包** (`BusinessEngineeringEditor`) - 编辑项目弹窗底部
- ✅ **项目总览** (`BusinessProjectEditor`) - 编辑项目弹窗底部

**触发方式**：编辑已有文章/项目时，在编辑弹窗底部右侧点击"历史记录"按钮

**显示内容**：单篇文章/项目的版本历史（`mode="editor"`，`useArticleVersionLabels={true}`）

**显示条件**：仅当编辑已有项目时显示（`showHistory={true}`）

---

### 7.4 模块/子项历史记录入口

#### 7.4.1 Tab 旁边的历史记录按钮

**位置类型**：Tab 切换区域右侧

**适用页面**：
- ✅ **企业公开 - 基本信息** (`PublicBasicEditor`) - Tab 区域右侧
  - 每个 Tab（工商注册、企业资质、发展战略、业务分布、工程业绩）都有独立的历史记录
  - 历史记录按钮显示在 Tab 切换区域右侧，与当前激活的 Tab 相关

**触发方式**：点击 Tab 区域右侧的"历史记录"按钮

**显示内容**：当前激活 Tab 的历史记录（`mode="editor"`）

#### 7.4.2 模块编辑弹窗中的历史记录按钮

**位置类型**：`PageBanner` 组件右侧（在模块编辑弹窗中）

**适用页面**：
- ✅ **实业投资** (`BusinessIndustryEditor`) - 编辑模块弹窗顶部右侧
- ✅ **国际贸易** (`BusinessTradeEditor`) - 编辑模块弹窗顶部右侧
- ✅ **科技创新** (`SustainTechEditor`) - 编辑模块弹窗顶部右侧
- ✅ **全球发展** (`SustainGlobalEditor`) - 编辑团队结构弹窗顶部右侧

**触发方式**：在模块编辑弹窗中，点击 `PageBanner` 右侧的"历史记录"按钮

**显示内容**：该模块的版本历史（`mode="editor"`，`useArticleVersionLabels={true}`）

---

### 7.5 卡片/列表项历史记录入口

#### 7.5.1 卡片右上角历史记录按钮（悬停显示）

**位置类型**：卡片右上角，鼠标悬停时显示

**适用页面**：
- ✅ **企业文化 - 精彩文化活动** (`CultureCompanyEditor`) - 活动卡片右上角
  - 位置：卡片右上角操作按钮组中（编辑、历史记录、删除）
  - 显示条件：鼠标悬停在卡片上时显示（`opacity-0 group-hover:opacity-100`）
  - 仅当活动已存在时显示（`act.id && activities.find(a => a.id === act.id)`）

**触发方式**：鼠标悬停在活动卡片上，点击右上角的时钟图标按钮

**显示内容**：该活动的版本历史（`mode="editor"`，`useArticleVersionLabels={true}`）

#### 7.5.2 编辑弹窗底部历史记录按钮

**位置类型**：编辑弹窗底部右侧（与"确认"按钮并排）

**适用页面**：
- ✅ **企业文化 - 精彩文化活动** (`CultureCompanyEditor`) - 活动编辑弹窗底部
  - 位置：弹窗底部按钮栏右侧，"确认"按钮左侧
  - 显示条件：仅当编辑已有活动时显示（`editingActivity.id && activities.find(a => a.id === editingActivity.id)`）

**触发方式**：在活动编辑弹窗中，点击底部右侧的"历史记录"按钮

**显示内容**：该活动的版本历史（`mode="editor"`，`useArticleVersionLabels={true}`）

---

### 7.6 历史记录入口汇总表

| 入口类型 | 位置 | 适用页面 | 模式 | 显示条件 |
|---------|------|---------|------|---------|
| **PageBanner 右侧** | 页面顶部右侧 | 所有配置页面、资源库页面、文章管理页面 | editor/library/article | 始终显示 |
| **编辑弹窗底部** | 弹窗底部右侧 | 文章/项目编辑弹窗 | editor | 仅编辑已有项目时显示 |
| **卡片右上角** | 卡片悬停时显示 | 企业文化-活动卡片 | editor | 仅已有活动时显示 |
| **Tab 区域右侧** | Tab 切换区域右侧 | 企业公开-基本信息 | editor | 始终显示 |
| **模块编辑弹窗** | 模块编辑弹窗顶部 | 实业投资、国际贸易、科技创新、全球发展 | editor | 始终显示 |

---

### 7.7 历史记录按钮样式规范

所有历史记录按钮统一使用以下样式：

**图标**：`Clock` 图标（`lucide-react`）
**文字**："历史记录"
**样式类**：
```jsx
className="flex items-center gap-1.5 px-3 py-2 text-gray-6 hover:text-gray-7 hover:bg-gray-2 rounded-lg text-sm transition-colors"
```

**图标尺寸**：
- 标准按钮：`w-4 h-4`
- 小尺寸按钮（PageBanner）：`w-3.5 h-3.5`
- 卡片操作按钮：`w-3.5 h-3.5`

---

## 八、历史记录恢复功能

### 8.1 支持恢复的场景

- ✅ 配置页面模式（`mode="editor"`）- 可恢复到任意历史版本
  - **限制**：仅 `status='draft'` 的记录可恢复（保存记录）
  - **不可恢复**：`status='published'`、`status='updated'`、`status='unpublish'` 的记录不可恢复
- ❌ 文章页面级操作模式（`mode="article"`）- **不支持恢复**（页面级操作日志，不可逆）
- ❌ 资源库页面级操作日志（`mode="library"`）- **不支持恢复**（操作不可逆）
- ✅ 资源库单个资源版本历史（`VersionHistoryModal`）- **支持恢复**（可恢复到任意历史版本）

### 8.2 恢复流程

1. 用户在历史记录弹窗中选择要恢复的版本
2. 点击"恢复此版本"按钮
3. 系统弹出确认提示
4. 确认后，将历史版本的数据加载到编辑器
5. 用户需要手动保存和发布才能生效

### 8.3 恢复限制

- 只能恢复非最新版本的记录
- 恢复后需要重新保存和发布
- 恢复操作本身不会创建新的历史记录

---

## 九、历史记录筛选功能

### 9.1 搜索功能

- **搜索范围**：修改描述、操作人
- **搜索方式**：模糊匹配，不区分大小写
- **实时搜索**：输入即搜索

### 9.2 时间筛选

- **全部时间**：显示所有记录
- **最近7天**：显示最近7天的记录
- **最近30天**：显示最近30天的记录
- **最近3个月**：显示最近3个月的记录

### 9.3 状态/操作筛选

**配置页面模式**：
- 全部状态
- 发布上线
- 保存配置

**资源库页面级操作日志**：
- 全部操作
- 新增
- 编辑
- 删除

**文章页面级操作模式**：
- 全部操作
- 上架
- 下架
- 展示位移动

---

## 十、历史记录显示规范

### 10.1 时间显示格式

- **列表时间**：`MM-DD HH:mm`，如："03-20 14:30"
- **月份分组**：`YYYY年MM月`，如："2024年3月"
- **详细时间**：`YYYY-MM-DD HH:mm`，如："2024-03-20 14:30"

### 10.2 状态标签颜色

**配置页面模式**：
- 🟢 **发布上线** (`published`) - 绿色背景 (`bg-success/10`)，绿色文字 (`text-success`)
- 🔵 **保存配置** (`draft`) - 蓝色背景 (`bg-brand/10`)，蓝色文字 (`text-brand`)

**资源库页面级操作日志**：
- 🟢 **新增** (`add`) - 绿色背景，绿色文字
- 🔵 **编辑** (`edit`) - 蓝色背景，蓝色文字
- 🔴 **删除** (`delete`) - 红色背景，红色文字

**资源库单个资源版本历史**（`VersionHistoryModal`）：
- 🔵 **当前版本** - 蓝色圆点（`bg-brand`），蓝色版本号文字，显示"当前"标签
- ⚪ **历史版本** - 灰色圆点（`bg-gray-4`），黑色版本号文字，显示"恢复"按钮

**文章页面级操作模式**：
- 🟢 **发布** (`publish`) - 绿色背景 (`bg-success/10`)，绿色文字 (`text-success`)
- 🔴 **下架** (`unpublish`) - 红色背景 (`bg-error/10`)，红色文字 (`text-error`)
- 🔵 **更新** (`update`) - 蓝色背景 (`bg-brand/10`)，蓝色文字 (`text-brand`)

**单篇文章版本历史**（`mode="editor"` + `useArticleVersionLabels={true}`）：
- 🟢 **发布** (`published-article`) - 绿色背景，绿色文字
- 🔵 **保存** (`draft-article`) - 蓝色背景，蓝色文字
- 🔵 **更新** (`updated`) - 蓝色背景，蓝色文字
- 🔴 **下架** (`unpublish-article`) - 红色背景，红色文字

### 10.3 时间轴显示

- 最新记录：蓝色实心圆点 (`bg-brand`)
- 历史记录：蓝色半透明圆点 (`bg-brand/30`)，悬停时加深 (`bg-brand/60`)
- 时间轴线条：灰色边框 (`border-gray-4`)

---

## 十一、应用页面汇总表

**说明**：
- ✅ 表示支持恢复历史版本
- ✅* 表示支持恢复，但仅 `status='draft'`（保存）的记录可恢复
- ❌ 表示不支持恢复

| 模块 | 页面 | 组件 | 页面标识 | 模式 | 支持恢复 |
|------|------|------|----------|------|----------|
| **全局资源库** |
| | 公司档案信息-页面级 | CompanyInfoEditor | `company_info` | library | ❌ |
| | 公司档案信息-单个资源 | CompanyInfoEditor | `company_info_item_{id}` | version | ✅ |
| | 图片媒体库 | ImageLibraryEditor | `image_library` | library | ❌ |
| | 资质荣誉库-页面级 | CertLibraryEditor | `cert_library` | library | ❌ |
| | 资质荣誉库-单个资源 | CertLibraryEditor | `cert_library_item_{id}` | version | ✅ |
| | 人员专家库 | PeopleLibraryEditor | `people_library` | library | ❌ |
| **首页管理** |
| | Banner设置 | HomeBannerEditor | `home_banner` | editor | ✅ |
| | 最新动态 | HomeNewsEditor | `home_news` | editor | ✅ |
| | 关于我们 | HomeAboutEditor | `home_about` | editor | ✅ |
| | 业务板块 | HomeBusinessEditor | `home_business` | editor | ✅ |
| | 社会责任与文化 | HomeCultureEditor | `home_culture` | editor | ✅ |
| | 员工风采 | HomeStaffEditor | `home_staff` | editor | ✅ |
| | 合作伙伴 | HomePartnerEditor | `home_partner` | editor | ✅ |
| **关于我们** |
| | Banner设置 | AboutBannerEditor | `about_banner` | editor | ✅ |
| | 公司简介 | IntroEditor | `about_intro` | editor | ✅ |
| | 发展历程 | HistoryEditor | `about_history` | editor | ✅ |
| | 董事长致辞 | SpeechEditor | `about_speech` | editor | ✅ |
| | 公司领导 | LeadersEditor | `about_leaders` | editor | ✅ |
| | 荣誉资质 | HonorsEditor | `about_honors` | editor | ✅ |
| **新闻中心** |
| | Banner设置 | NewsBannerEditor | `news_banner` | editor | ✅ |
| | 文章管理-页面级 | NewsListEditor | `news_list_page` | article | ❌ |
| | 文章管理-单篇 | NewsListEditor | `news_article_{id}` | editor | ✅* |
| **业务领域** |
| | Banner设置 | BusinessBannerEditor | `business_banner` | editor | ✅ |
| | 工程承包-页面级 | BusinessEngineeringEditor | `business_engineering_page` | article | ❌ |
| | 工程承包-单项目 | BusinessEngineeringEditor | `business_project_{id}` | editor | ✅* |
| | 项目总览-页面级 | BusinessProjectEditor | `business_project_page` | article | ❌ |
| | 项目总览-单项目 | BusinessProjectEditor | `business_project_{id}` | editor | ✅* |
| | 实业投资 | BusinessIndustryEditor | `business_industry` | editor | ✅ |
| | 国际贸易 | BusinessTradeEditor | `business_trade` | editor | ✅ |
| **品牌文化** |
| | Banner设置 | ModuleBannerEditor | `culture_banner` | editor | ✅ |
| | 企业文化 | CultureCompanyEditor | `culture_company` | editor | ✅ |
| | 企业文化-单活动 | CultureCompanyEditor | `culture_activity_{id}` | editor | ✅* |
| | 品牌形象 | BrandImageEditor | `culture_brand` | editor | ✅ |
| **党建领航** |
| | Banner设置 | ModuleBannerEditor | `party_banner` | editor | ✅ |
| | 党的建设 | PartyBuildingEditor | `party_building` | editor/article | ✅ |
| | 廉洁从业 | PartyIntegrityEditor | `party_integrity` | editor | ✅ |
| | 青年之友 | PartyYouthEditor | `party_youth` | editor/article | ✅ |
| | 职工之家 | PartyWorkersEditor | `party_workers` | editor | ✅ |
| **人力资源** |
| | Banner设置 | ModuleBannerEditor | `hr_banner` | editor | ✅ |
| | 人才战略 | HREditor | `hr_strategy` | editor | ✅ |
| | 人才队伍 | HREditor | `hr_team` | editor | ✅ |
| | 招聘信息 | HRRecruitEditor | `hr_recruit` | library/article | ❌/✅ |
| **企业公开** |
| | Banner设置 | ModuleBannerEditor | `public_banner` | editor | ✅ |
| | 基本信息 | PublicBasicEditor | `public_basic` | editor | ✅ |
| | 联系方式 | PublicContactEditor | `public_contact` | editor | ✅ |
| **可持续发展** |
| | Banner设置 | ModuleBannerEditor | `sustain_banner` | editor | ✅ |
| | 科技创新 | SustainTechEditor | `sustain_tech` | editor | ✅ |
| | 科技创新-成果 | SustainTechEditor | `sustain_tech_result` | library | ❌ |
| | 安环行动 | SustainSafetyEditor | `sustain_safety` | editor | ✅ |
| | 全球发展 | SustainGlobalEditor | `sustain_global` | editor | ✅ |
| | 全球发展-团队 | SustainGlobalEditor | `sustain_global_team` | library | ❌ |
| | 社会责任 | SustainResponsibilityEditor | `sustain_responsibility` | editor | ✅ |
| | ESG报告 | SustainESGEditor | `sustain_esg` | editor | ✅ |
| **其他** |
| | 页脚内容 | FooterContentEditor | `footer_content` | editor | ✅ |

---

## 十二、开发注意事项

### 12.1 历史记录生成时机

- **保存配置**：用户点击"保存配置"按钮时立即生成 `draft` 记录
- **发布更新**：用户点击"发布更新"按钮时立即生成 `published` 记录
- **资源操作**：用户执行新增/编辑/删除操作时立即生成对应记录（页面级操作日志）
- **资源编辑**：用户编辑资源项时，同时生成该资源的版本历史记录（单个资源版本历史）
- **文章操作**：用户执行上架/下架/移动操作时立即生成对应记录

### 12.2 历史记录描述生成

- **系统自动生成**：根据修改的字段自动生成描述
- **用户自定义**：允许用户在保存时输入自定义描述（可选功能）
- **默认描述**：如果无法自动生成，使用默认描述

### 12.3 历史记录数据快照

- **建议保存**：保存完整的数据快照，用于恢复功能
- **存储格式**：JSON 字符串
- **数据大小**：注意控制快照大小，避免存储过大

### 12.4 历史记录清理

- **本地存储**：最多保存最近 100 条记录，超出自动删除最旧记录
- **后端存储**：建议保留至少 1 年的历史记录
- **定期清理**：定期清理过期的历史记录

---

## 十三、未来扩展计划

### 13.1 功能增强

- [ ] 支持历史记录导出（Excel/CSV）
- [ ] 支持历史记录对比（查看两个版本的差异）
- [ ] 支持批量恢复历史版本
- [ ] 支持历史记录评论/备注功能

### 13.2 性能优化

- [ ] 历史记录分页加载（避免一次性加载过多数据）
- [ ] 历史记录懒加载（按需加载数据快照）
- [ ] 历史记录索引优化（提高搜索和筛选速度）

### 13.3 用户体验

- [ ] 历史记录操作动画效果
- [ ] 历史记录操作快捷键
- [ ] 历史记录操作统计图表

---

## 十四、总结

CMS 系统的历史记录功能覆盖了所有内容管理场景，包括：

1. **配置页面**：记录配置的保存和发布历史，支持版本恢复
2. **资源库**：记录资源的新增、编辑、删除操作
3. **文章管理**：记录文章的上架、下架、展示位移动操作

通过统一的历史记录组件 `UnifiedHistoryModal`，系统提供了：
- ✅ 完整的操作追踪
- ✅ 灵活的筛选和搜索
- ✅ 版本恢复功能
- ✅ 清晰的时间轴展示

历史记录功能为 CMS 系统提供了完整的审计追踪能力，帮助用户了解内容的变更历史，并在需要时恢复到之前的版本。

---

---

## 十五、历史记录按钮位置详细说明

### 15.1 按钮位置类型汇总

| 位置类型 | 组件/位置 | 适用场景 | 示例页面 |
|---------|----------|---------|---------|
| **PageBanner 右侧** | 页面顶部右侧 | 所有配置页面、资源库、页面级操作日志 | 公司档案、首页管理、关于我们等 |
| **编辑弹窗底部** | 弹窗底部右侧 | 编辑已有文章/项目/活动时 | 新闻中心-文章编辑、工程承包-项目编辑 |
| **卡片右上角** | 卡片悬停时显示 | 卡片列表中的操作按钮 | 企业文化-活动卡片 |
| **Tab 区域右侧** | Tab 切换区域右侧 | 多 Tab 页面的历史记录 | 企业公开-基本信息 |
| **模块编辑弹窗** | 模块编辑弹窗顶部 | 模块编辑时的历史记录 | 实业投资、国际贸易、科技创新 |

### 15.2 各页面历史记录按钮详细位置

#### 15.2.1 使用 PageBanner 右侧按钮的页面

所有使用 `EditorLayout` 或 `PageBanner` 组件的页面，历史记录按钮统一显示在页面顶部右侧。

**完整列表**：
- 公司档案信息
- 首页管理（所有子页面）
- 关于我们（所有子页面）
- 新闻中心 - Banner设置
- 业务领域 - Banner设置、实业投资、国际贸易
- 品牌文化 - Banner设置、品牌形象
- 党建领航 - Banner设置
- 人力资源 - Banner设置、人才战略、人才队伍
- 企业公开 - Banner设置、联系方式
- 可持续发展（所有子页面）
- 页脚内容
- 图片媒体库
- 资质荣誉库
- 人员专家库
- 新闻中心 - 文章管理（页面级操作）
- 工程承包（页面级操作）
- 项目总览（页面级操作）

#### 15.2.2 使用编辑弹窗底部按钮的页面

**新闻中心 - 文章管理** (`NewsListEditor`)
- 位置：文章编辑弹窗底部右侧
- 显示条件：仅当编辑已有文章时显示（`editingNews.id && newsList.find(n => n.id === editingNews.id)`）
- 模式：`mode="editor"`，`useArticleVersionLabels={true}`

**工程承包** (`BusinessEngineeringEditor`)
- 位置：项目编辑弹窗底部右侧
- 显示条件：仅当编辑已有项目时显示
- 模式：`mode="editor"`，`useArticleVersionLabels={true}`

**项目总览** (`BusinessProjectEditor`)
- 位置：项目编辑弹窗底部右侧
- 显示条件：仅当编辑已有项目时显示
- 模式：`mode="editor"`，`useArticleVersionLabels={true}`

#### 15.2.3 使用卡片右上角按钮的页面

**企业文化 - 精彩文化活动** (`CultureCompanyEditor`)
- 位置：活动卡片右上角操作按钮组（编辑、历史记录、删除）
- 显示条件：
  1. 鼠标悬停在卡片上时显示（`opacity-0 group-hover:opacity-100`）
  2. 仅当活动已存在时显示（`act.id && activities.find(a => a.id === act.id)`）
- 模式：`mode="editor"`，`useArticleVersionLabels={true}`

#### 15.2.4 使用 Tab 区域右侧按钮的页面

**企业公开 - 基本信息** (`PublicBasicEditor`)
- 位置：Tab 切换区域右侧，与 Tab 按钮水平对齐
- 显示条件：始终显示（`shouldShowHistory` 为 true 时）
- 模式：`mode="editor"`
- 特殊说明：每个 Tab 有独立的历史记录

#### 15.2.5 使用模块编辑弹窗按钮的页面

**实业投资** (`BusinessIndustryEditor`)
- 位置：模块编辑弹窗中的 `PageBanner` 右侧
- 模式：`mode="editor"`，`useArticleVersionLabels={true}`

**国际贸易** (`BusinessTradeEditor`)
- 位置：模块编辑弹窗中的 `PageBanner` 右侧
- 模式：`mode="editor"`，`useArticleVersionLabels={true}`

**科技创新** (`SustainTechEditor`)
- 位置：模块编辑弹窗中的 `PageBanner` 右侧
- 模式：`mode="editor"`，`useArticleVersionLabels={true}`

**全球发展** (`SustainGlobalEditor`)
- 位置：团队结构编辑弹窗中的 `PageBanner` 右侧
- 模式：`mode="editor"`

---

**文档版本**：v2.0  
**最后更新**：2025-01-XX  
**维护人**：开发团队

**更新说明**：
- v2.0：完善历史记录按钮位置说明，更新文章页面级操作模式描述，修正恢复功能说明
- v1.0：初始版本


